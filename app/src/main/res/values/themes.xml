<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeatherApp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/poppins_medium</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="Theme.WeatherApp" parent="Base.Theme.WeatherApp" />
    <style name="CustomAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/customDialogBackground</item>
        <item name="android:textColorPrimary">@color/customDialogTitle</item>
        <item name="android:textColor">@color/customDialogText</item>
        <item name="colorAccent">@color/customDialogButtonPositive</item>
        <item name="android:textColorSecondary">@color/customDialogButtonNegative</item>
    </style>
</resources>